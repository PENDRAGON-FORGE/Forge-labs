name: Build FORGE ASCENT APK

on:
  pull_request:
    branches: ["forge-ascent-ci-base"]
    paths:
      - "apps/forge-ascent/**"
      - ".github/workflows/build-forge-ascent-apk.yml"
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/forge-ascent

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/forge-ascent/package.json

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Restore verified FORGE ASCENT v2 web bundle
        run: |
          rm -rf www bundle-v2.b64 bundle-v2.zip
          cat > payload-v2/SHA1SUMS <<'EOF'
          0df492cb643bb8ace8c5e9d60de05df9da699bbc  payload-v2/p01.b64
          a2e48fa8d4b08aa0d02ee7f41f96d64e34e18e17  payload-v2/q02.b64
          450851ee4d9f8954d40d2326587f8150147af6ef  payload-v2/q03.b64
          a24a24c0bc7f99bbe22789993603ab344d3e2dce  payload-v2/q04.b64
          cf1444bad7cb2176e0feff2b43b748849e498792  payload-v2/t05_1.b64
          7b28bc2a34eb8bb905771e2a8f19626c4ba29813  payload-v2/t05_2.b64
          bdaa73f3d5dd4f7bb3d4dbcb67b3c3cfd1548e3c  payload-v2/t05_3.b64
          17b2b3cd82a91502f0782637434259c0856202f6  payload-v2/t05_4.b64
          6a21170f15bb66c147cb8efaebfea268dc247cbb  payload-v2/s06a.b64
          30ca16520af39e08ea4e144c88f085af59860f90  payload-v2/s06b.b64
          4c53af0b63c1b25a45b13328eea08dcd09a58a0d  payload-v2/s07a.b64
          d355322c0fcb82badb468fda37038898215d3bb0  payload-v2/s07b.b64
          EOF
          sha1sum -c payload-v2/SHA1SUMS
          cat payload-v2/p01.b64 payload-v2/q02.b64 payload-v2/q03.b64 payload-v2/q04.b64 payload-v2/t05_1.b64 payload-v2/t05_2.b64 payload-v2/t05_3.b64 payload-v2/t05_4.b64 payload-v2/s06a.b64 payload-v2/s06b.b64 payload-v2/s07a.b64 payload-v2/s07b.b64 > bundle-v2.b64
          base64 --decode bundle-v2.b64 > bundle-v2.zip
          echo "b04029b6579866ac171f50a6765d30019d576f5a1c88ccaf89ad2eea49bf40ab  bundle-v2.zip" | sha256sum -c -
          unzip -t bundle-v2.zip
          mkdir -p www
          unzip -q bundle-v2.zip -d www
          test -f www/index.html
          test -f www/app.js

      - name: Apply onboarding runtime fix v2.0.1
        run: |
          python3 - <<'PY'
          from pathlib import Path

          ds = Path("www/data-store.js")
          s = ds.read_text(encoding="utf-8")
          s = s.replace(
              '    schemaVersion: SCHEMA_VERSION,\n    profile: {',
              '    schemaVersion: SCHEMA_VERSION,\n    ui: { onboardingComplete: false },\n    profile: {',
              1,
          )
          s = s.replace(
              '    } else {\n      Object.assign(d.profile, source.profile||{});',
              '    } else {\n      Object.assign(d.ui, source.ui||{});\n      Object.assign(d.profile, source.profile||{});',
              1,
          )
          s = s.replace(
              '      schemaVersion:SCHEMA_VERSION,\n      profile:{...d.profile,...(s.profile||{})},',
              '      schemaVersion:SCHEMA_VERSION,\n      ui:{...d.ui,...(s.ui||{})},\n      profile:{...d.profile,...(s.profile||{})},',
              1,
          )
          s = s.replace(
              '    out.profile.age = Math.floor(number(out.profile.age,13,100,33));',
              '    out.ui.onboardingComplete = Boolean(out.ui.onboardingComplete);\n    out.profile.age = Math.floor(number(out.profile.age,13,100,33));',
              1,
          )
          ds.write_text(s, encoding="utf-8")

          app = Path("www/app.js")
          a = app.read_text(encoding="utf-8")
          a += r'''

          // FORGE ASCENT v2.0.1 — late onboarding binding fix.
          (function bindForgeAscentOnboardingAfterDom(){
            function bind(){
              const currentStep=()=>Number(document.querySelector('.onboarding-step:not(.hidden)')?.dataset.step||0);
              document.querySelectorAll('.onboarding-next').forEach(btn=>{
                btn.type='button';
                btn.onclick=()=>showOnboardingStep(currentStep()+1);
              });
              document.querySelectorAll('.onboarding-prev').forEach(btn=>{
                btn.type='button';
                btn.onclick=()=>showOnboardingStep(currentStep()-1);
              });
              const finish=document.getElementById('onboardingFinish');
              if(finish){
                finish.type='button';
                finish.onclick=finishOnboarding;
              }
            }
            if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',bind,{once:true});
            else bind();
          })();
          '''
          app.write_text(a, encoding="utf-8")
          PY
          node --check www/app.js
          node --check www/data-store.js
          grep -q "bindForgeAscentOnboardingAfterDom" www/app.js
          grep -q "onboardingComplete" www/data-store.js

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Add Android platform
        run: npx cap add android

      - name: Set Android version
        run: |
          sed -i 's/versionCode 1/versionCode 21/' android/app/build.gradle
          sed -i 's/versionName "1.0"/versionName "2.0.1"/' android/app/build.gradle

      - name: Sync web assets
        run: npx cap sync android

      - name: Build native debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace

      - name: Copy APK
        run: cp android/app/build/outputs/apk/debug/app-debug.apk FORGE_ASCENT_v2.0.1-native.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: FORGE_ASCENT_v2.0.1_NATIVE_APK
          path: apps/forge-ascent/FORGE_ASCENT_v2.0.1-native.apk
          if-no-files-found: error
          retention-days: 30
